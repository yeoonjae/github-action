name: Hello World

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    # - uses: actions/checkout@v2
    - name: Run pwd
      run: pwd
    - name: Build with Gradle
      uses: Run ls -al
      run: ls -al
